enum Action {
  ClockInAndScheduleClockOut = "clock-in-and-schedule-clock-out",
  DoNotScheduleClockOut = "do-not-schedule-clock-out",
  ClockOut = "clock-out",
  ScheduleClockOut = "schedule-clock-out",
  DoNotClockIn = "do-not-clock-in",
}

export default Action;
